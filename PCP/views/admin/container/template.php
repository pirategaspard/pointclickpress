<?php 

	//echo($page->id);

	echo($story_info);
	echo($container_info);
	echo($container_form);
	if ($container->id > 0)
	{
		echo($event_add); 
		echo($event_list); 	 
		echo($scene_add); 
		echo($scene_list); 	
	}
?>
