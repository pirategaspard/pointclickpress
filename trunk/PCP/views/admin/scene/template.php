<?php 

	//echo($page->id);

	echo($story_info);
	echo($container_info);
	echo($scene_info);
	echo($add_scene);
	echo($scene_form);
	echo($scene_grid);
	//echo($actions_form);
	if ($scene->id > 0)
	{ 
		echo($add_action); 
		echo($action_form); 	
	}
	
?>
