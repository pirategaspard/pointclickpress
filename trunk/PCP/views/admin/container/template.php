<?php 

	//echo($page->id);

	echo($story_info);
	echo($container_info);
	echo($container_form);
	echo($event_add); 
	echo($event_list); 
	if ($container->id > 0)
	{ 
		echo($scene_add); 
		echo($scene_list); 	
	}
?>
